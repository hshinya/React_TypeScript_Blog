import { BrowserRouter, Routes, Route } from "react-router-dom";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/posts" element={} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
